{
    "name": "molotov",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "start": "cd front && npm run start",
        "build": "cd front && ng serve"
    },
    "dependencies": {
        "@angular/compiler-cli": "^4.0.0",
        "@angular/cli": "^7.0.0"
    },
    "devDependencies": {
        "@angular/compiler-cli": "^4.0.0",
        "@angular/cli": "^7.0.0"
    }
}