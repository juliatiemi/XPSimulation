{
    "name": "molotov",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "start": "cd front && npm run start && cd ../",
        "build": "pushd front && ng serve && popd"
    }
}